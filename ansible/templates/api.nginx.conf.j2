server {
    listen 80;
    server_name {{ api.subdomain }}.{{ domain }};

    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:8080/;
    }
    
    location ~ /\.well-known/acme-challenge {
        allow all;
    }
}