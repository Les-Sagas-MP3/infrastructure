all:
  hosts:
    ${environment_name}:
      ansible_host: ${ip_address}
      db:
        name: lessagasmp3
        username: lessagasmp3
        password: lessagasmp3
      api:
        url: ${api_url}
      app:
        url: ${app_url}
